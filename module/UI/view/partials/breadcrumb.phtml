<?php

foreach ($this->pages as $page) {
    $css_class = '';

    if ($page->isActive()) {
        $css_class = 'current';
    }

    echo <<<HEREDOC
<div class="breadcrumb_divider"></div>
<a class="$css_class" href="{$page->getHref()}">
    {$this->translate($page->getLabel())}
</a>

HEREDOC;
}